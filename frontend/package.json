{
  "name": "athlete-assessment-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.8.1", // For routing
    "react-scripts": "5.0.1",
    "axios": "^1.3.2", // For API calls
    "papaparse": "^5.3.2", // Added for CSV parsing in DataEntryPage
    "chart.js": "^4.2.1", // Added for charts
    "react-chartjs-2": "^5.2.0", // Added for React wrapper for Chart.js
    "react-toastify": "^9.1.1", // Added for toast notifications
    "web-vitals": "^2.1.4"
    // Add other UI libraries here as needed: Material UI, Ant Design, TailwindCSS, etc.
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "proxy": "http://localhost:5001" // Proxy backend API calls in development
}
